machine:
  pre:
    - sudo apt-get install libgmp-dev libmpfr-dev gyp libflint-2.5.2 gcc g++ automake autoconf

test:
  pre:
    - gyp --depth=. --no-duplicate-basename-check
    - make
  override:
    - out/Default/test
